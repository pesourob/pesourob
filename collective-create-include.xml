<?xml version="1.0" encoding="UTF-8" ?>
<server description="This file was generated by the 'collective create' command on 2024-11-24 16:15:06 GMT.">
    <featureManager>
        <feature>collectiveController-1.0</feature>
    </featureManager>

    <!-- Define the host name for use by the collective.
         If the host name needs to be changed, the server should be
         removed from the collective and re-joined or re-replicated. -->
    <variable name="defaultHostName" value="ebce4313f85d" />


    <!-- TODO: Set the security configuration for Administrative access -->
    <quickStartSecurity userName="admin" userPassword="password123" />

    <!-- clientAuthenticationSupported set to enable bidirectional trust -->
    <ssl id="defaultSSLConfig"
         keyStoreRef="defaultKeyStore"
         trustStoreRef="defaultTrustStore"
         clientAuthenticationSupported="true" />

    <!-- inbound (HTTPS) keystore -->
    <keyStore id="defaultKeyStore" password="{xor}Lz4sLCgwLTtubWw="
              location="${server.config.dir}/resources/security/key.p12" />

    <!-- inbound (HTTPS) truststore -->
    <keyStore id="defaultTrustStore" password="{xor}Lz4sLCgwLTtubWw="
              location="${server.config.dir}/resources/security/trust.p12" />

    <!-- server identity keystore -->
    <keyStore id="serverIdentity" password="{xor}Lz4sLCgwLTtubWw="
              location="${server.config.dir}/resources/collective/serverIdentity.p12" />

    <!-- collective trust keystore -->
    <keyStore id="collectiveTrust" password="{xor}Lz4sLCgwLTtubWw="
              location="${server.config.dir}/resources/collective/collectiveTrust.p12" />

    <!-- collective root signers keystore -->
    <keyStore id="collectiveRootKeys" password="{xor}Lz4sLCgwLTtubWw="
              location="${server.config.dir}/resources/collective/rootKeys.p12" />
    </server>
